import { CheckEstadoTruePipe } from './check-estado-true.pipe';

describe('CheckEstadoTruePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckEstadoTruePipe();
    expect(pipe).toBeTruthy();
  });
});
